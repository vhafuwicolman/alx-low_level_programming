#include "main.h"

/**
 * main - tests function that prints if integer is positive or nagetive
 * Return: 0 when successful
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
